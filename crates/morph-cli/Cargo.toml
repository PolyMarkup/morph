[package]
name = "morph-cli"
version.workspace = true
edition.workspace = true
description = "Command-line interface for the Morph universal markup converter"
license.workspace = true
repository.workspace = true

[[bin]]
name = "morph"
path = "src/main.rs"

[dependencies]
morph = { path = "../morph-core" }
